{% extends "base.html.j2" %}
{% import 'bootstrap/wtf.html' as wtf %}
{% block app_content %}
    <h1>{{ _('登入「我的戶口」') }}</h1>
    <div class="row">
        <div class="col-md-4">{{ wtf.quick_form(form) }}</div>
    </div>
    <br/>
    <p>
        <button ><a href="{{ url_for('register') }}">{{ _('一次性密碼登入') }}</a></button>
    </p>
    <p>
        {{ _('New User?') }} <a href="{{ url_for('register') }}">{{ _('首次登入') }}</a>
    </p>
    <p>
        {{ _('Forgot Your Password?') }}
        <a href="{{ url_for('reset_password_request') }}">{{ _('訪客登記') }}</a>
    </p>
{% endblock %}
